import React from "react";

const className = "vuz";

export default function Vuz() {
  return (
    <div className={className}>
      <div className={`${className}-main`}>vuz</div>
    </div>
  );
}
